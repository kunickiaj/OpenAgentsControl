# ProseWriter evals

These tests cover concise revision and non-technical drafting for the domain-neutral `ProseWriter` agent.

```sh
npm run eval:sdk -- --agent=content/prose-writer --pattern="**/smoke-test.yaml"
npm run eval:sdk -- --agent=content/prose-writer --pattern="**/general-draft.yaml"
```
